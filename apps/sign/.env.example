# SIGN Platform - Frontend Environment Variables

VITE_API_URL=http://localhost:3000/api/v1
# DEPRECATED — referenced in .env.example but not used in code.
# Kept to avoid breaking existing local setups. Safe to ignore.
VITE_AI_API_URL=http://localhost:3000/api/v1/ai
VITE_SOCKET_URL=http://localhost:3000
VITE_DEFAULT_LANGUAGE=en

# URL of the ManageX app — used for backlinks in Sign layouts
VITE_MANAGEX_URL=http://localhost:5175
